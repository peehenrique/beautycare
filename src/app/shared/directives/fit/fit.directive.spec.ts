import { FitDirective } from './fit.directive';

describe('FitDirective', () => {
  it('should create an instance', () => {
    const directive = new FitDirective();
    expect(directive).toBeTruthy();
  });
});
