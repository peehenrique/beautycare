import { OnlyLettersAndPunctuationDirective } from './only-letters-and-punctuation.directive';

describe('OnlyLettersAndPunctuationDirective', () => {
  it('should create an instance', () => {
    const directive = new OnlyLettersAndPunctuationDirective();
    expect(directive).toBeTruthy();
  });
});
